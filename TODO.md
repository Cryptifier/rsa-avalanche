# TODO

1. Add holdout validation for oracle screening to avoid overfitting the per-bit rankings. Implementation: in `src/bin/analysis.rs` split `OracleTrainingSample` into train/validation sets (e.g., 80/20) and rank `screen_oracles_per_bit` by validation accuracy while still using the training set to estimate inversion; add config keys like `engine.oracle_screen_holdout_pct` and log both train/val stats in analytics.
2. Replace unweighted per-bit voting with weighted voting based on oracle reliability. Implementation: compute per-oracle per-bit match rates from `screen_oracles_per_bit` and pass weights into a new `weighted_vote_with_distribution` in `src/combiner.rs`; use those weights in `run_bitwise_speculative_oracle_attempt` so the recovered bits reflect stronger oracles, aiming to push accuracy beyond 52%.
3. Add confidence-aware inversion logic for per-bit oracle selection. Implementation: instead of flipping any oracle with `match_pct < 50`, compute a Wilson interval or binomial test in `screen_oracles_per_bit` and only invert when the upper bound is still below 50%; store the interval in analytics to justify flipping.
4. Make `top_k` adaptive per bit based on separation between candidates. Implementation: when ranking for a bit, include the oracle only while the cumulative majority probability improves by at least `engine.oracle_selection_margin` (new config); this reduces weak oracles that dilute signal.
5. Use multi-shift ensembles for prediction rather than only for visualization. Implementation: extend `build_bit_similarity_entries` to also return per-shift oracle bits and combine votes across shifts with weights (e.g., higher weight for shifts with higher adjusted match %).
6. Introduce a Bayesian prior from the match timeline to bias oracle votes. Implementation: compute a per-bit prior from `match_series.bit_true_prob` (mean over frames) and combine it with oracle likelihoods to form a posterior bit prediction in `run_bitwise_speculative_oracle_attempt`.
7. Add r-candidate scoring that incorporates structure (bit length, factor count, min factor) to prefer candidates that historically yield >52% accuracy. Implementation: define a scoring function in `src/r_candidates.rs` and use it in `build_oracle_candidates` to pre-rank candidates before screening.
8. Support mixed candidate pools across generation modes. Implementation: in `build_oracle_candidates`, generate candidates in both `Factoring` and `SmallPrimes` modes (or multiple settings) and merge/dedup the pool; expose config toggles like `engine.r_candidate_mix`.
9. Export per-bit confusion matrices for screening. Implementation: augment `screen_oracles_per_bit` to track true/false positives/negatives for each bit and write a CSV in `run_information_sufficiency_tests` so accuracy gains beyond 52% are shown per bit.
10. Add explicit statistical significance tests for >52% accuracy. Implementation: compute a binomial test p-value for the overall match rate and per-bit rates in `run_information_sufficiency_tests`, and require p < threshold in sufficiency checks.
11. Add calibration and reliability plots for `probability_one`. Implementation: bin predicted probabilities from `MajorityDistribution.probability_one` and compare to actual match rates; export a CSV and plot in `scripts/enciphered_bins_video.py` or new plotting helper in `src/bin/analysis.rs`.
12. Implement an oracle diversity selector to reduce correlated errors. Implementation: after ranking, greedily select `top_k` oracles that maximize Hamming distance on the screening samples; add a config flag `engine.oracle_diversity_enable` to toggle.
13. Add structured message generators to test recovery of known structure above 52%. Implementation: extend `MessageConfig` in `src/config.rs` with patterns (fixed prefix/suffix, repeated blocks, low-entropy templates), and update `random_message_under_n` in `src/bin/analysis.rs` to support them.
14. Add per-bit priors for known structure (e.g., ASCII ranges, fixed headers). Implementation: supply a `engine.bit_priors` array in config and apply those as priors in the Bayesian vote step so structural bits are biased correctly.
15. Track oracle stability across runs and re-use stable oracles. Implementation: persist per-oracle accuracy history to `reuse_r_candidates_path` metadata or a new JSON, and in `build_oracle_candidates` bias toward candidates with stable performance.
16. Add a reproducible screening dataset export/import. Implementation: serialize `OracleTrainingSample` to a CSV/JSON in `run_information_sufficiency_tests` and add a `--screening-data` flag to reuse the same messages for fair comparisons.
17. Implement a grid-search runner for r candidate parameters. Implementation: add a script in `scripts/` that sweeps `r_candidate_bit_length`, `small_prime_factors`, and `process_min_factor`, runs `analysis --tests`, and aggregates `speculative_match_pct` into a summary CSV.
18. Integrate ramp signal strength into oracle weighting. Implementation: use `src/dsp.rs` ramp strength from `enciphered_export_*` data as an additional signal and increase weights for candidates whose ramp features correlate with higher match %.
19. Add sequential testing to stop early when evidence exceeds 52% with confidence. Implementation: use a SPRT-style check inside `run_match_entropy_timeline` to stop sampling once confidence bounds exceed 52% (or below 48% for inversion), speeding runs while still reporting significance.
20. Improve analytics output for bit-true probability trends. Implementation: extend `SessionAnalytics` in `src/analytics.rs` to store per-bit trajectories, and add a small viewer enhancement (or CSV export) to visualize which bits consistently surpass the 52% threshold across frames.
